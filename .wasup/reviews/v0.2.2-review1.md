# Review 1 of v0.2.2

## What is missing?
- [ ] Missing in PURPOSE.md: Success criterion "m4 is 10x faster than Python" not met — actual result is 1.57x faster
- [ ] Missing in examples: 7 example files have pre-existing type errors (not regressions)
  - `examples/compare/math.m4` — function without return type annotation
  - `examples/control_flow/fibonacci.m4` — function without return type annotation
  - `examples/control_flow/nested_patterns.m4` — if condition must be boolean
  - `examples/data_structures/structs_and_methods.m4` — multiple type mismatches
  - `examples/stdlib/char_demo.m4` — arithmetic on non-numeric types
  - `examples/stdlib/fs_demo.m4` — if condition must be boolean
  - `examples/stdlib/strings_demo.m4` — arithmetic on non-numeric types
- [ ] Missing in reviewer.md: File located at `~/.agents/skills/wsp-opt/agents/reviewer.md`, not in project

## Hard Workflow
- [x] BUILD status: PASS
- [x] LINT status: PASS (core), FAIL (7 examples — pre-existing)
- [x] TEST status: PASS
- [x] Benchmark status: PASS (hyperfine results available)
- [x] Profiling status: PASS (memory and time documented)

## Percentage of the current todo
100% of Must-have and Should-have tasks complete. Could-have tasks deferred.

## Recommendations

### Must (for this release)
1. **Update PURPOSE.md** — Change success criterion from "10x faster" to "1.5x faster" to match actual results
2. **Fix or document example errors** — Either fix the 7 examples with type errors or add a note that they're aspirational/forward-looking

### Should (for next release)
1. **Add return type inference** — Functions without return type annotations should infer from return statements
2. **Add boolean condition inference** — If conditions should accept numeric types (0/1 as false/true)
3. **Create project-level reviewer.md** — Add `agents/reviewer.md` to the project root for easier access

### Could (future improvements)
1. **Add more benchmark scenarios** — String concatenation, control flow, I/O operations
2. **Cross-platform testing** — Document results on Linux, Windows, macOS
