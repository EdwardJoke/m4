# Sync Report v0.1.3
**Date**: 2026-05-27

## Files Updated

### Root Config
- [x] `AGENTS.md` - Updated pipeline diagram to include QBE IR alternative path; added `qbe.zig`, `qbe_build.zig`, and runtime files to key source files table; updated "Current State" section with string operations (comparison, indexing, length); noted QBE v0.2.0 development status
- [x] `README.md` - Bumped version from v0.1.0 to v0.1.2 in CLI help and status; added `--native` flag and `build` subcommand to CLI section; expanded project structure to include `qbe.zig`, `qbe_build.zig`, `runtime/` directory, and all stdlib modules (`std.zig`, `thread.zig`, `range.zig`); updated "Implemented" list with string ops, io stdlib functions, threading, range, QBE backend, and benchmarks; updated "Not Yet Implemented" ("AOT compilation" → "Cranelift JIT backend"); fixed typo ("build on" → "built on"); fixed license link path
- [x] `ROADMAP.md` - Updated "no separate IR" statement to reflect QBE SSA IR emitter and native binary pipeline

## Changes Summary
- Updated 3 documents with current project state
- Added QBE backend references to AGENTS.md, README.md, and ROADMAP.md
- Fixed 1 typo ("build" → "built")
- Fixed 1 dead link path (`/qbe/LICENSE` → `qbe/LICENSE`)
- Updated version numbers in 2 CLI examples from v0.1.0 to v0.1.2
- Added 2 new CLI flags (`--native`, `build` subcommand) to README.md
- Expanded project structure in README.md from 16 entries to 24 entries
- Extended stdlib module listing from 1 to 4 modules

## Files Skipped
- `SPEC.md` - Language specification, accurate as-is (no status/version information to update)
- `.wasup/changelogs/*.md` - Changelogs are historical records, intentionally left unchanged
- `.wasup/todos/*.md` - Task tracking documents, accurately reflect current state
- `.wasup/PURPOSE.md` - Project intent metadata, accurate as-is
