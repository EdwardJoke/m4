# Sync Report v0.2.1
**Date**: 2026-06-01

## Files Updated

### Root Config
- [x] `AGENTS.md` — Updated status from v0.1.2 to v0.2.1; replaced `--check` flag references with `lint` subcommand (3 occurrences); updated QBE status from "in development" to "functional"
- [x] `README.md` — Replaced CLI help text with current `m4 help` output (removed `--check`, `--error-format`, `-h`, `-v` flags; added `Commands:` section with `help`/`version`/`lint`/`build`/`explain` subcommands, `Build options:` section with `-o`/`-target`); bumped version from v0.2.0 to v0.2.1; updated QBE status description
- [x] `ROADMAP.md` — Updated current state version from v0.2.0 to v0.2.1
- [x] `SPEC.md` — Bumped draft version from v0.1 to v0.2

### Project Metadata
- [x] `.wasup/PURPOSE.md` — Updated from v0.2.1 (completed) to v0.2.2 (next iteration placeholder)

## Changes Summary
- Updated 5 documents to reflect v0.2.1 release
- Replaced `--check` global flag with `lint` subcommand across all documentation
- Synced CLI help text in README.md to match actual `m4 help` output (new `Commands:` structure, build options, removed `--error-format=`, `-h`, `-v` flags)
- Bumped version references from v0.1.2/v0.2.0 to v0.2.1 in status headers and descriptions

## Files Skipped
- `.wasup/changelogs/*.md` — Historical records, intentionally unchanged
- `.wasup/todos/*.md` — Task tracking, accurately reflect current state
- `.wasup/gates/*.md` — Gate reports, historical records
