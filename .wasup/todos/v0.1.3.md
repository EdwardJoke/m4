# Todo
> Target Version: v0.1.3 | Mode: Bug (Minor)

## Must have
- [x] M1: String type + operations (comparison, length, indexing) - Core foundation
- [x] M2: String I/O (std.println/std.readln for strings) - Already working

## Should have
- [x] S1: Format CLI flag (`--format`) - Already working, verified output

## Could have
- [x] C1: Better error messages - Updated vm error text and error DB descriptions

## Summary of changes
### VM (`src/vm.zig`)
- **String comparison**: Refactored `cmpSlow` into `cmpSlow` → `cmpOrder` → `cmpValues`. Added `.string` case with `std.mem.order(u8, ...)` for lexicographic comparison of `<`, `>`, `<=`, `>=`
- **String length** (`index_len`): Added `.string` case returning byte length
- **String indexing** (`index_get`): Added `.string` case returning `.char` at index
- Improved error messages: "cannot compare non-numeric types" → "cannot compare values of different types"

### Error registry (`src/error.zig`)
- Updated r005, r008, r009 descriptions to mention string support
