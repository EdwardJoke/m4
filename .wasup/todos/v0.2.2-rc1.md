# Todo
> Target Version: v0.2.2-rc1 | Mode: Bug (Minor)

## Must have
- [ ] **NEXT** M1: Fix known bugs — triage and fix issues found in v0.2.1 (CI, edge cases, runtime crashes)
- [ ] M2: Update AGENTS.md — bump status to v0.2.2, reflect current state
- [ ] M3: Remove ROADMAP.md — content absorbed into AGENTS.md or SPEC.md; update AGENTS.md accordingly
- [ ] M4: Test suite passes clean — `zig build test` + verify examples

## Should have
- [ ] S1: Build `fs` module — `fs.read(path)`, `fs.write(path, data)`, `fs.exists(path)`, `fs.delete(path)` with clean simple names
- [ ] S2: CI reliability — finalize CI workflow, ensure green on push

## Could have
- [ ] C1: Additional stdlib additions (string utilities like `str.len`, `str.slice`)
- [ ] C2: Optimize CLI help output consistency

## Won't have (this version / defer to v0.3.0)
- [ ] W1: Complex string functions (split, join, regex)
- [ ] W2: Recursion support — targets self-hosting, not rc scope
- [ ] W3: Full `std.ord`/`std.chr` conversion functions (defer with fs module as priority)
