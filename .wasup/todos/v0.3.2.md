# Todo
> Target Version: v0.3.2 | Mode: Patch (Major) — -D flag + unboxed int QBE backend

## Must have
- [x] M1: Add `-D` flag to m4 CLI for QBE optimization levels — Expose `-Dfast` (fast compile, large binary) and `-Dsmall` (slow compile, small binary) options
- [x] M2: Change emitIntLit to emit `%r =l copy <value>` instead of `call $m4_new_int` in QBE backend
- [x] M3: Change emitBinary to emit native QBE ops (add/sub/mul/div/rem, ceql/csltl/etc.) instead of runtime calls for unboxed ints
- [x] M4: Update m4rt.c so m4_new_int returns raw l values; add m4_box_int for boxing at type boundaries
- [x] M5: Add boxed/unboxed tracking system in QBE emitter with auto-boxing at type boundaries (function calls, vec/struct stores, index/field access, if conditions)

## Should have
- [x] S1: Update help text to document `-D` flag with fast/small descriptions
- [x] S2: Add unboxed entry points in runtime (m4_add_u, m4_sub_u, etc.) for int-only operations

## Could have
- [x] C1: Add validation for `-D` values (reject unknown options)
- [x] C2: Optimize if-stmt conditions to use jnz directly for unboxed values instead of calling m4_is_truthy

## Won't have (this version / defer to v0.3.3)
- [ ] W1: Additional QBE backend flags beyond `-Dfast`/`-Dsmall`
- [ ] W2: Float unboxing — floats still use M4Value* heap allocation
