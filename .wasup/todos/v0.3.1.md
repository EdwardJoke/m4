# Todo
> Target Version: v0.3.1 | Mode: Bug (Minor) — Fix all open issues

## Must have
- [ ] **NEXT** M1: Fix integer overflow semantics (#4) — Use Zig's `*WithOverflow` builtins to give arithmetic defined overflow behavior, returning a runtime error
- [ ] M2: Fix global cache LRU implementation (#5) — Promote entries to front on both hits and misses; right-shift entries with position 3 as eviction target
- [ ] M3: Add memory management for heap-allocated objects (#6) — Implement proper deinitialization in VM, compiler, stdlib, and QBE runtime to free heap objects

## Should have
(None — all bugs are Must-haves)

## Could have
(None)

## Won't have (this version / defer to v0.3.2)
- [ ] W1: New language features
- [ ] W2: Full garbage collector (mark-and-sweep or ref-counting) — scope limited to proper deinit cleanup

