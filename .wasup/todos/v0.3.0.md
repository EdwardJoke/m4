# Todo
> Target Version: v0.3.0 | Mode: Patch (Major) — Documentation & Stability

## Must have
- [x] M1: Remove `io` module — all io functions consolidated into `std`; `src/stdlib/io.zig` deleted; all references updated
- [x] M2: Verify both runtimes — `std.zig` has all 6 io functions; `m4rt.c`/`m4rt.h` clean of io aliases; `println`/`print`/`readln` verified working with `use std`
- [x] M3: Update all `.m4` examples/tests to use `std.` instead of `io.` — all 28 .m4 files already clean; updated SPEC.md and cli.zig comment
- [ ] M4: Write complete SPEC.md docs for all public stdlib APIs (`std`, `fs`, `str`, `thread`, `range`)
- [ ] M5: Polish CLI — fix Zig error leak (P1), unify error format across stages (P2), ensure non-zero exit codes for errors (P5)

## Should have
- [ ] S1: Expand ERROR_DB in `src/error.zig` — add missing error codes for common failure modes
- [ ] S2: Write docstrings/comments for all public Zig API functions

## Deferred (v0.4.0)
- [ ] D1: Source locations for type errors (P3)
- [ ] D2: Auto-register `std` module in stdin/file mode (P4)

## Could have
- [ ] C1: Add `--pretty` flag for colored/readable error output
- [ ] C2: Update README.md with v0.3.0 changes

## Won't have (this version / defer to v0.4.0)
- [ ] W1: New language features (bitwise ops, result type runtime support)
- [ ] W2: `map[K V]` generic container implementation
