# Todo
> Target Version: v0.3.3 | Mode: Bug

## Must have
- [x] M1: Fix runtime memory management — shallow teardown in m4_free_value, correct signature, nil-safe div_u/mod_u
- [ ] **NEXT** M2: Fix qbe_wrap — reset opt before each compilation to prevent state leaking
- [ ] M2: Fix qbe_wrap — reset opt before each compilation to prevent state leaking
- [ ] M3: Fix QBE backend — track BoxKind instead of bool in ensureBoxed for correct non-int boxing
- [ ] M4: Fix qbe_build — hash-based runtime object cache keyed by source+target instead of file existence
- [ ] M5: Fix parser — structured diagnostics for lex errors (p001), propagate had_error
- [ ] M6: Fix CLI — improve lint error format output
- [ ] M7: Fix thread — add dedicated Value tags for thread_handle and channel
- [ ] M8: Fix stdlib — resolve @constCast warnings, update Io usage, fix stringBuilder leak
- [ ] M9: Fix test runner — stable exit code capture, script portability (macOS/Linux)

## Should have
- [ ] S1: Add test coverage — parser p002/p004 paths, CLI -D flag parsing
- [ ] S2: Add test coverage — m4 unit tests with fail-count tracking instead of print-based assertions
- [ ] S3: Version bump and documentation updates
