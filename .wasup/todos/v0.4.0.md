# Todo
> Target Version: v0.4.0 | Mode: Function

## Must have
- [ ] **NEXT** M1: Restructure repo — move compiler `.zig` files to `src/compiler/`, create `src/toolchain/`
- [ ] M2: Update `build.zig` to build both `m4c` (compiler) and `mein` (toolchain)
- [ ] M3: Rename `m4` binary to `m4c` — compiler-only binary
- [ ] M4: Extract non-compiler CLI functions into `mein` toolchain binary
- [ ] M5: Implement `mein new project` / `mein init project` scaffolding

## Should have
- [ ] S1: Implement `mein clean cache` command
- [ ] S2: Update docs/references from `m4` to `m4c` where compiler-specific
- [ ] S3: Update examples, scripts, CI to use `m4c`

## Could have
- [ ] C1: Shell completion for `m4c` and `mein`
- [ ] C2: Polished `mein --version` / `mein --help` output

## Won't have (defer to v0.5.0)
- [ ] W1: Package management
