# Todo
> Target Version: v0.4.0 | Mode: Function

## Must have
- [x] M1: Restructure repo — move compiler `.zig` files to `src/compiler/`, create `src/toolchain/`
- [x] M2: Update `build.zig` to build both `m4c` (compiler) and `mein` (toolchain)
- [x] M3: Rename `m4` binary to `m4c` — compiler-only binary
- [x] M4: Extract non-compiler CLI functions into `mein` toolchain binary
- [x] M5: Implement `mein new project` / `mein init project` scaffolding

## Should have
- [x] S1: Implement `mein clean cache` command
- [x] S2: Update docs/references from `m4` to `m4c` where compiler-specific
- [x] S3: Update examples, scripts, CI to use `m4c`

## Could have
- [x] C1: Shell completion for `m4c` and `mein`
- [x] **NEXT** C2: Polished `mein --version` / `mein --help` output

## Won't have (defer to v0.5.0)
- [ ] W1: Package management
